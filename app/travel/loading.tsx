// Blank template — travel route loading state.
// The Temporal Loom intro animation lives in page.tsx, which is the direct
// entry point for /travel (same pattern as /projects → radar → /projects/maiia).

export default function TravelLoading() {
  return (
    <div className="fixed inset-0 z-[60] bg-[#050505]" />
  );
}
